# 🗺️ 横断山区 → 新疆绿洲 地图项目部署指南

> 日期：2026年7月31日 - 8月5日  
> 服务器：DigitalOcean Droplet (新加坡)  
> 项目仓库：https://github.com/Palpi-tate/Map  
> 正式域名：https://zhangynteam.xyz（腾讯云购买 + Let's Encrypt HTTPS）

---

## 📦 项目信息

| 项目 | 地址 |
|------|------|
| **GitHub 仓库** | `https://github.com/Palpi-tate/Map` |
| **Droplet IP** | `167.71.192.92` |
| **Droplet 名称** | `ubuntu-s-2vcpu-2gb-90gb-intel-sgp1` |
| **SSH 登录** | 通过 DigitalOcean Web Console |
| **✅ 正式 HTTPS 地址** | `https://zhangynteam.xyz`（永久稳定） |
| **备用 HTTPS 地址** | `https://saturday-earwig-unlisted.ngrok-free.dev`（ngrok 隧道） |

---

## 🖼️ 项目页面列表

| 页面 | HTTPS 地址 |
|------|-----------|
| 🍑 首页（桃之起源） | `https://zhangynteam.xyz/index.html` |
| 🍑 桃·东传北京 | `https://zhangynteam.xyz/tao2.html` |
| 🍑 桃·南下江南 | `https://zhangynteam.xyz/tao3.html` |
| 🌍 桃·西传世界 | `https://zhangynteam.xyz/tao-world.html` |
| 🌸 马嵬遗恨 | `https://zhangynteam.xyz/mawei.html` |
| 🌸 盛唐牡丹 | `https://zhangynteam.xyz/tang.html` |

---

## 🏗️ 服务器环境

- **系统**：Ubuntu 24.04 (Noble)
- **Web 服务器**：Nginx 1.24.0
- **网站根目录**：`/var/www/html/`
- **防火墙**：DigitalOcean 云防火墙 `ai-hack6`

---

## 🔧 部署步骤回顾

### 1. 在服务器安装 Nginx

```bash
sudo apt update && sudo apt install nginx -y
```

### 2. 拉取代码

```bash
cd /var/www/html && sudo rm -rf ./* && sudo git clone https://github.com/Palpi-tate/Map.git .
sudo systemctl restart nginx
```

### 3. 解决 iptables 重定向问题

发现问题：80 端口流量被重定向到 8888 端口导致 502 错误

```bash
# 查看规则
sudo iptables -t nat -L -n

# 删除重定向规则
sudo iptables -t nat -D PREROUTING -p tcp --dport 80 -j REDIRECT --to-port 8888
sudo iptables -t nat -D PREROUTING -p tcp --dport 80 -j REDIRECT --to-port 8888
```

### 4. 配置防火墙

- 进入 `Networking → Firewalls → ai-hack6`
- Inbound Rules 添加：HTTP 80, SSH 22, Custom TCP 8888
- Droplets 标签页中添加 Droplet

---

## 🖼️ 地图瓦片配置

### 当前使用：高德地图（国内秒开）

```javascript
const osmLayer = new ol.layer.Tile({
  source: new ol.source.XYZ({
    url: "https://webrd0{1-4}.is.autonavi.com/appmaptile?lang=zh_cn&size=1&scale=1&style=8&x={x}&y={y}&z={z}"
  })
});
```

> ⚠️ 高德地图使用 GCJ-02（火星坐标系），与 WGS-84 有约 300-500m 偏移。  
> 如需精确 WGS-84，可换回 OSM 瓦片或使用天地图。

### 备用（OSM 瓦片，国外快国内慢）

```javascript
const osmLayer = new ol.layer.Tile({
  source: new ol.source.OSM({
    url: 'https://{a-c}.tile.openstreetmap.org/{z}/{x}/{y}.png'
  })
});
```

---

## 🌐 HTTPS 隧道（ngrok）【已被域名方案取代，保留备用】

### 为什么需要（原背景）

- GeoScene StoryMaps 是 HTTPS 站点，不允许嵌入 HTTP 页面
- DigitalOcean Droplet 直接暴露 HTTP（80 端口），没有 SSL 证书

### 安装 ngrok

```bash
curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
sudo apt update && sudo apt install ngrok -y
```

### 配置

```bash
# 登录 ngrok.com → Your Authtoken → 复制 token
ngrok config add-authtoken 你的token
```

### 后台运行（关终端不断）

```bash
nohup ngrok http 80 --log=stdout > /dev/null 2>&1 &
```

### 查看当前隧道地址

```bash
curl -s http://127.0.0.1:4040/api/tunnels | grep public_url
```

### ⚠️ ngrok 的缺点（最终改用域名方案的原因）

- 免费版每次重启地址会变
- 免费版有**访问警告页**（"You are about to visit..."），嵌入 iframe 时会显示警告
- 解决方案对比：ngrok 付费 $8/月 > 买域名 + Let's Encrypt 免费证书（更划算）

---

## 🌟 正式域名 + HTTPS 方案（最终采用）

> 2026年8月5日完成。买腾讯云 `.xyz` 域名（首年 15 元），配 Let's Encrypt 免费 HTTPS，**无警告页、永久稳定**。

### 1. 购买域名

- 平台：腾讯云（DNSPod）→ `buy.cloud.tencent.com/domain`
- 购买的域名：`zhangynteam.xyz`（首年 15 元）
- ⚠️ **只买域名**，不要加购：轻量服务器(40元/月)、DNS解析(39.9元/年)、SSL证书(64.6元/年)——都用不上
- ⚠️ 国内域名需要**实名认证**（身份证），审核通过前解析不生效
- 服务器在海外 → **不需要 ICP 备案**

### 2. 配置 DNS 解析（腾讯云 DNSPod 控制台）

1. 登录腾讯云 → 云解析 DNS → 找到域名
2. 点 **添加记录**，加两条：

| 主机记录 | 记录类型 | 记录值 |
|---------|---------|--------|
| `@` | A | `167.71.192.92` |
| `www` | A | `167.71.192.92` |

3. 验证解析是否生效（Droplet 或本地均可）：

```bash
nslookup zhangynteam.xyz
# 返回 Address: 167.71.192.92 即生效
```

> 如果 nslookup 报 NXDOMAIN 或解析不到，可能是：实名认证未通过 / 刚加的记录还没全球生效（等 10-30 分钟）

### 3. 安装 certbot 并申请证书

```bash
# 安装
sudo apt update && sudo apt install certbot python3-certbot-nginx -y

# 申请证书（自动配置 Nginx HTTPS）
sudo certbot --nginx -d zhangynteam.xyz -d www.zhangynteam.xyz --agree-tos -m 你的邮箱
```

成功标志（输出类似）：
```
Successfully received certificate.
Certificate is saved at: /etc/letsencrypt/live/zhangynteam.xyz/fullchain.pem
Congratulations! You have successfully enabled HTTPS on https://zhangynteam.xyz
```

### 4. ⚠️ 关键：DigitalOcean 防火墙必须开 443 端口

**这是最容易漏的一步！** 证书配好了但 443 没放行，浏览器访问会超时：

- `Networking → Firewalls → ai-hack6`
- Inbound Rules 添加：**HTTPS (443) / All IPv4 / All IPv6**
- 防火墙规则最终应为：SSH 22、HTTP 80、HTTPS 443、Custom 8888

### 5. 证书自动续期（certbot 已自动配置）

```bash
# 验证定时任务存在
sudo systemctl list-timers | grep certbot
```

### 6. 验证（Droplet 内）

```bash
# 域名 HTTPS 正常
curl -I https://zhangynteam.xyz
# 应返回 HTTP/1.1 200 OK，且带 Content-Security-Policy 头

# ngrok 隧道不受影响（同一台服务器、同一目录）
curl -H "Host: saturday-earwig-unlisted.ngrok-free.dev" http://localhost -I
# 应返回 200 OK
```

---

## 🔄 日常更新流程

```bash
# 1. 本地修改代码后
git add index.html
git commit -m "描述修改内容"
git push

# 2. Droplet 上拉取更新
cd /var/www/html && sudo git pull

# 3. 如果 Droplet 重启了，需要重新启动 ngrok（如果用 ngrok 备用）
nohup ngrok http 80 --log=stdout > /dev/null 2>&1 &
```

> ✅ 域名 HTTPS 证书会自动续期，无需手动操作（certbot 定时任务）

---

## 🛠️ 常用诊断命令

```bash
# 检查 Nginx 状态
sudo systemctl status nginx

# 检查 Nginx 配置
sudo nginx -t

# 查看网站文件
ls -la /var/www/html/

# 查看访问日志
sudo tail -20 /var/log/nginx/access.log

# 查看错误日志
sudo tail -20 /var/log/nginx/error.log

# 检查 80/443 端口监听
sudo ss -tlnp | grep -E ':(80|443)'

# 本地测试 HTTP
curl -I http://localhost

# 测试域名 HTTPS
curl -I https://zhangynteam.xyz

# 检查证书
sudo certbot certificates

# 手动续期证书
sudo certbot renew --dry-run

# 查看 iptables 规则
sudo iptables -t nat -L -n

# 查看防火墙
sudo ufw status

# 查看 ngrok 隧道地址
curl -s http://127.0.0.1:4040/api/tunnels | grep public_url
```

---

## 🗂️ Nginx 配置文件

路径：`/etc/nginx/sites-enabled/default`

> ⚠️ certbot 申请证书后会自动修改此文件，加入 443 监听和证书路径。下面是配置要点：

```nginx
# 域名 80 -> 443 跳转（可选，certbot --redirect 或手动添加）
server {
    listen 80;
    server_name zhangynteam.xyz www.zhangynteam.xyz;
    return 301 https://$host$request_uri;
}

# 主站点（HTTP + HTTPS 都服务 /var/www/html）
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    listen 443 ssl;
    listen [::]:443 ssl;

    ssl_certificate /etc/letsencrypt/live/zhangynteam.xyz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/zhangynteam.xyz/privkey.pem;

    root /var/www/html;
    index index.html index.htm index.nginx-debian.html;
    server_name zhangynteam.xyz www.zhangynteam.xyz _;
    add_header Content-Security-Policy "frame-ancestors https://www.geosceneonline.cn" always;

    location / {
        try_files $uri $uri/ =404;
    }
}
```

> 💡 **为什么要保留 `default_server`**：ngrok 隧道请求的 Host 头是 ngrok 域名，靠 default_server 兜底匹配，这样域名和 ngrok 能同时工作。

---

## 🚀 后续可选优化

1. ✅ ~~买域名 + Let's Encrypt HTTPS~~ **已完成**（zhangynteam.xyz，2026-08-05）
2. **GeoScene 嵌入**：把 StoryMap 里的 ngrok 地址全部替换为 `https://zhangynteam.xyz/xxx.html`
3. **清理**：域名方案稳定后可停掉 ngrok（`pkill ngrok`），省资源
4. **固定 IP**：如需长期使用，可购买 DigitalOcean Reserved IP 绑定 Droplet
5. **国内加速**：若国内访问仍慢，可加 Cloudflare CDN 代理（免费）

---

## 📝 其他尝试过的方案

| 方案 | 结果 |
|------|------|
| Serveo 隧道 | ✅ 可行，但免费版有警告页 |
| Cloudflare Tunnel | ❌ 新加坡到 Cloudflare API 连接失败 |
| Cloudflare Pages | ⏸️ 未完成部署（Build command 问题） |
| 直接 HTTP 嵌入 | ❌ GeoScene HTTPS 禁止混合内容 |
| OSM 瓦片直接访问 | ⚠️ 国内不稳定，国外正常 |
